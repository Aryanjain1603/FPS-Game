using UnityEngine;

namespace Player
{
    public static class CursorLock
    {
        public static bool isCursorLock = true;
        public static void CursorLockStatus(bool status)
        {
            // if (status)
            // {
            //     Cursor.lockState = CursorLockMode.Locked; // Locks to center of screen
            //     Cursor.visible = false;
            // }
            // else
            // {
            //     Cursor.lockState = CursorLockMode.None; // Locks to center of screen
            //     Cursor.visible = true; // Hides the cursor

            // }
            
        }
        
    }
}
